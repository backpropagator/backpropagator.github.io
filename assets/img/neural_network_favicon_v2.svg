<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <!-- Gradient matching the website's theme (Blue to Purple) -->
    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#5e72e4;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#7c3aed;stop-opacity:1" />
    </linearGradient>
    
    <!-- Subtle glow for the nodes -->
    <filter id="glow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="1" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>
  
  <!-- Background: Rounded Square for high visibility -->
  <rect x="0" y="0" width="100" height="100" rx="22" ry="22" fill="url(#bgGradient)" />
  
  <!-- Neural Network Structure (2-3-1 Topology) -->
  <g stroke="#ffffff" stroke-width="5" stroke-linecap="round" fill="#ffffff" filter="url(#glow)">
    <!-- Connections (Lines) -->
    <!-- Input (Left) to Hidden (Center) -->
    <!-- From Input 1 (Top) -->
    <line x1="20" y1="30" x2="50" y2="20" opacity="0.6" />
    <line x1="20" y1="30" x2="50" y2="50" opacity="0.6" />
    <line x1="20" y1="30" x2="50" y2="80" opacity="0.6" />
    
    <!-- From Input 2 (Bottom) -->
    <line x1="20" y1="70" x2="50" y2="20" opacity="0.6" />
    <line x1="20" y1="70" x2="50" y2="50" opacity="0.6" />
    <line x1="20" y1="70" x2="50" y2="80" opacity="0.6" />
    
    <!-- Hidden (Center) to Output (Right) -->
    <line x1="50" y1="20" x2="80" y2="50" opacity="0.8" />
    <line x1="50" y1="50" x2="80" y2="50" opacity="0.8" />
    <line x1="50" y1="80" x2="80" y2="50" opacity="0.8" />
    
    <!-- Nodes (Circles) -->
    <!-- Input Layer (2 Nodes) -->
    <circle cx="20" cy="30" r="7.5" />
    <circle cx="20" cy="70" r="7.5" />
    
    <!-- Hidden Layer (3 Nodes) -->
    <circle cx="50" cy="20" r="7.5" />
    <circle cx="50" cy="50" r="7.5" />
    <circle cx="50" cy="80" r="7.5" />
    
    <!-- Output Layer (1 Node) -->
    <circle cx="80" cy="50" r="9.5" />
  </g>
</svg>
